import 'package:flutter/material.dart';

class IndexActor extends StatelessWidget {
  const IndexActor({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}